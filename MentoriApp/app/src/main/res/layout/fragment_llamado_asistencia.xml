<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:background="#FFFFFF"
    tools:context=".Mentor.MentorLlamadoAsistenciaFragment">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- Título -->
        <ImageButton
            android:id="@+id/tv_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/baseline_arrow_back_24"
            android:textSize="18sp"
            android:textStyle="bold"
            android:background="#FFFFFF"
            android:paddingBottom="16dp"
            android:textColor="#000000" />

        <!-- Tema -->
        <TextView
            android:id="@+id/tv_topic_label"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Tema:"
            android:textStyle="bold"
            android:textColor="#000000" />

        <EditText
            android:id="@+id/et_topic"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Recursión en C"
            android:padding="8dp"
            android:background="@drawable/border_card" />

        <!-- Descripción -->
        <TextView
            android:id="@+id/tv_description_label"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Descripción:"
            android:textStyle="bold"
            android:paddingTop="16dp"
            android:textColor="#000000" />

        <EditText
            android:id="@+id/et_description"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Escribe una descripción..."
            android:padding="8dp"
            android:background="@drawable/border_card"
            android:minLines="3"
            android:gravity="top"
            android:inputType="textMultiLine" />

        <!-- Contenedor dinámico para CheckBox -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/tv_mentees_label"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:paddingTop="16dp"
                android:text="Lista de mentoriados"
                android:textColor="#000000"
                android:textStyle="bold" />

            <ImageButton
                android:id="@+id/imageButton"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_gravity="right"
                android:layout_weight="1"
                android:foregroundGravity="right"
                android:backgroundTint="#FFFFFF"
                android:src="@drawable/baseline_photo_camera_24" />
        </LinearLayout>

        <LinearLayout
            android:id="@+id/container_checkboxes"
            android:layout_width="match_parent"
            android:layout_height="354dp"
            android:orientation="vertical"
            android:paddingTop="8dp">
            <!-- CheckBoxes serán añadidos dinámicamente desde la base de datos -->
        </LinearLayout>

        <!-- Botón para cerrar asistencia -->
        <Button
            android:id="@+id/btn_close_attendance"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Cerrar Asistencia"
            android:backgroundTint="#c41ff9"
            android:textColor="#FFFFFF"
            android:padding="12dp"
            android:layout_marginTop="16dp" />
    </LinearLayout>

</FrameLayout>

